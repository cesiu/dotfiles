\input{stdclassic}

\usepackage[bottom]{footmisc}
\usepackage{lastpage}
\usepackage{cprotect}
\usepackage{verbatim}
\usepackage{cancel}
\usepackage{caption}
\usepackage{skak}



\geometry{
    top=1.5in,
    bottom=1in,
    left=1.25in,
    right=1.25in,
}



\clearpairofpagestyles
\cfoot[\pagemark{}]{\pagemark{}}
\ihead[]{\textsc{\theauthor : \thedoc}}
\ohead[]{\textsc{\thedate}}

\titleformat{\section}{\relax}{\textbf{\uppercase{\thesection}}}{1em}{\bfseries\lsstyle\uppercase}

% NOTE: In an article, most code listings are inside an itemize...
\lstdefinestyle{cli}{
    numbers=none,
    frame=none,
    xleftmargin=2.125em,
    aboveskip=0.4375em,
    belowskip=0em
}

% NOTE: ...but occasionally we need listings outside an itemize.
\lstdefinestyle{exlst}{
    aboveskip=-0.4375em,
    belowskip=-0.8125em
}

\lstdefinestyle{excli}{
    numbers=none,
    frame=none,
    xleftmargin=2.5em,
    aboveskip=-0.4375em,
    belowskip=-0.8125em
}

% NOTE: This is used for code listings that are placed inside of a zero-height
%       solution box, so that they overlay some subsequent given code listing.
\lstdefinestyle{soln}{
    numbers=none,
    frame=none,
    aboveskip=0.8125em
}

\newboolean{@showsolns}
\newcommand{\showsoln}{\setboolean{@showsolns}{true}}
\newcommand{\hidesoln}{\setboolean{@showsolns}{false}}

% TODO: Do these need to be vboxes? Minipages result in runaway arguments when
%       combined with comments inside of a macro/environment def'n (and they
%       ruin the spaces anyways), but vboxes might be overkill (and they reset
%       the indentation too).
\newenvironment{solution}[1][]{
    \ifthenelse{\equal{#1}{}}{}{\vtop to #1}\bgroup
    \ifthenelse{\boolean{@showsolns}}{\color{red}\em}{\comment}
} {
    \ifthenelse{\boolean{@showsolns}}{}{\endcomment}
    \egroup
}

\newcommand{\solnraw}[2]{
    \ifthenelse{\boolean{@showsolns}}{#1}{#2}
}

\newcommand{\solninline}[1]{
    \solnraw{{\color{red}\em #1}}{#1}
}

\newenvironment{problems}{\begin{enumerate}[item-vspace=\baselineskip]}{\end{enumerate}}
\newenvironment{choices}{\begin{enumerate}[item-vspace=-0.5em, begin-vspace=-0.5em]}{\end{enumerate}}
