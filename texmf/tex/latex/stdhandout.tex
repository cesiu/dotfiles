% NOTE: These settings apply only to non-Beamer documents.

% Include general settings:
\input{stdheader.tex}

% Redefine pauses as no-ops:
\newcommand{\pause}{}

% Redefine beamer block background as white:
\definecolor{MedianUltraLightOrange}{gray}{1}

% Regular documents can use enumitem:
\usepackage{enumitem}

% Smaller bullet points:
\renewcommand{\bullet}{\boldsymbol{\cdot}}
\renewcommand\labelitemi{{\boldmath$\cdot$}}
\renewcommand\labelitemii{{\boldmath$\cdot$}}
\renewcommand\labelitemiii{{\boldmath$\cdot$}}

% Margins compatible with geometry:
\geometry{
    top=1.25in,
    bottom=1in,
    left=1in,
    right=1in
}

% Listing style for shell commands:
\lstdefinestyle{cli}{
    basicstyle=\normalsize\ttfamily,
    numbers=none,
    frame=none
}
